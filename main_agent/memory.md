# 마스터 에이전트 메모리

<!-- 이 파일은 마스터가 세션을 넘어 기억할 운영 규칙·학습·피드백을 누적하는 곳이다.
     반복 선호나 "기억해/다음부터는" 요청을 edit_file로 여기에 기록한다. 일시적 정보는 적지 않는다. -->

## 운영 규칙
# **tool 사용**
### tool 사용 이전에 tool에 넣을 수 있는 값인지 아닌지 체크 후 사용
### 엄격하게 판단 후 조건에 안맞으면 알맞은 값 생성 후 다시 tool 실행

# **작업경로 : /app/tmp/xcu_space**
### 파일 생성, 변경, 삭제 및 정리 등은 반드시 이 경로안에서 모두 실행
### 폴더 내부는 항상 깔끔하게 작업별로 정리 필요하다면 폴더 생성도 가능

## 학습된 선호 / 피드백
(아직 없음)
